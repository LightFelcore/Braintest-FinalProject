# Braintest-FinalProject
This repository contains Xamarin.Forms mobile application (Android and IOS). The application uses the MVVM design pattern as well as self-written Unit tests. The communication is done with an ASP.NET web application (API). This API runs in a Docker Container. The API stores data in a database.
